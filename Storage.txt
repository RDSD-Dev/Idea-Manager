SQL:
    listItems:
        id: AUTOINCREMENT
        title: VarChar(64)
        makeDate: Date
        completeDate: Date
        Type: Int:
            1: Basic
            2: Remake from make Date
            3: Remake from last completeDate
        remakeNum: Int
        category: VarChar(32)
        sortNum: Int
        isPinned: Boolean

AsyncStorage:
    appCategories:
        [[{Category Title}, {Category Color}]]
    
    appNotes:
        [[{Note Title}, {Note Category}, {Note Sort Number}, {isPinned}, {makeDate}, {editDate}]]

    app{Note Title}:
        {Note Text}

    app{Note Title}Pictures:
        [{Picture}]

Object:
    Categories:
        {title, color, indexes[]}
    List:
        {title, id, type, category, sortNum, isPinned, makeDate, completeDate, remakeNum}
    
    Note:
        {title, type, category, sortNum, isPinned, makeDate, editDate}