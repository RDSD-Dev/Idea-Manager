AsyncStorage:
    ideaManagerCategories:
        [{title: {Category Title}, color: {Category Color}, data: []}]

    ideaManagerData:
        [{title, type, category, sortingNum, isPinned, makeDate, remakeNum(if needed), complete date(ifNeeded)},
        {title, type, category, sortingNum, isPinned, makeDate, editDate}]
    
    ideaManagerNotes:
        [[{Note Title}, {Note Category}, {Note Sort Number}, {isPinned}, {makeDate}, {editDate}]]

    ideaManager{Note Title}:
        {Note Text}

    ideaManager{Note Title}Pictures:
        [{Picture}]

Object:
    Categories:
        {title, color, data:
            [List and Note objects]
        }
    List:
        {title, id, type, category, sortingNum, isPinned, makeDate, completeDate, remakeNum}
    
    Note:
        {title, type, category, sortingNum, isPinned, makeDate, editDate}